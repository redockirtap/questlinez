<!DOCTYPE html>
<html>
  <head>
    <title>questlinez</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class='bg-bg-main text-main-text text-sm text-center'>
    
    <%= render 'shared/navbar' %>

    
      
      <main class='flex mx-auto w-8/12 pt-12'>
        <aside class='ml-12 relative w-[164px] border-r border-border-main h-screen'>
          <div class='sticky left-0 top-12'>
           <%= render 'shared/sidebar' %>
          </div>
        </aside>

        <div class='flex flex-col w-[1000px]'>
          <div id="flash" class="flash">
            <%= render "shared/flash" %>
          </div>
          <%= yield %>
        </div>
      </main>
  </body>
</html>
